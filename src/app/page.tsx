import Button from "@/components/Button/Button";

export default function Home() {
  return (
    <div>
      <Button
        styleType='primary'
        disabled
        className='addWidth'
      >
        일단 버튼임
      </Button>
    </div>
  );
}
